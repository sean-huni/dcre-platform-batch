package za.co.fnb.dcre.crg;

import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Test fixture standing in for a real fleet application class. Every Boot application in the fleet
 * sits at {@code za.co.fnb.dcre.<svc>}, so this one's package leaf is {@code crg} and the identity
 * derived from it is {@code dcre-crg}.
 */
@SpringBootApplication
public class CrgApplication {
}
